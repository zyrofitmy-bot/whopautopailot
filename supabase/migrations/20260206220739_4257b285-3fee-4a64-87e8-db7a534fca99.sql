-- Update cron job to use execute-all-runs instead of execute-organic-runs
-- Cron jobs will be set up manually with the correct project URL after deployment
-- See setup guide for cron job SQL
SELECT 1; -- placeholder